<%= form_for(book) do |f| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Book's Title:" %>
    <br />
    <%= f.text_field :title, size: 50 %>
  </div>

  <br /> <br />

  <div class="field">
    <%= f.label "Book's author" %>
    <br />
    <%= f.text_field :author, size: 50 %>
  </div>
  <%= f.hidden_field :user_id, :value=>current_user.id %>
  <br />

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
